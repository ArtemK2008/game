# Milestone 002 - Align Spec Folder Structure

## Implemented
- Verified all current spec documents are located under `specs/`.
- Verified `specs/milestone/` exists for milestone records.
- Checked spec file naming for consistency against the lowercase-with-underscores rule.

## Changed
- Renamed `specs/README.md` to `specs/readme.md` to match the repository naming rule.
- Updated the repository instruction reference in `AGENTS.md` to the new lowercase spec path.
- Added this milestone note.

## Enables
- Keeps the spec tree structurally consistent and easier to reference in later milestones.

## Limitations
- No tests were added because this milestone only aligns spec structure and naming.
