namespace Survivalon.Core
{
    /// <summary>
    /// Enumerates the minimal music contexts currently used by the prototype.
    /// </summary>
    public enum MusicContextId
    {
        Calm,
        Gameplay,
    }
}
